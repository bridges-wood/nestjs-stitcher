{
  "name": "gateway",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/gateway/src",
  "projectType": "library",
  "tags": [],
  "targets": {
    "build": {
      "dependsOn": ["^build"],
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "cd packages/gateway && swc src -d dist --strip-leading-paths --config-file ../../.swcrc --ignore '**/*.spec.ts'",
          "tsc -p packages/gateway/tsconfig.lib.json --outDir packages/gateway/dist --tsBuildInfoFile packages/gateway/dist/tsconfig.lib.tsbuildinfo --emitDeclarationOnly --declaration"
        ],
        "parallel": false,
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --project gateway",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
